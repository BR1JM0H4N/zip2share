<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Material 3 DayNight theme — automatically switches between light and dark
        based on the system setting (AppCompatDelegate.MODE_NIGHT_FOLLOW_SYSTEM).

        On Android 12+, DynamicColors.applyToActivityIfAvailable() (called in
        MainActivity.onCreate) injects the user's system wallpaper palette
        (colorPrimary, colorSecondaryContainer, etc.) into this theme at runtime.
        On older devices, or if dynamic colour is unsupported, Material 3's
        built-in baseline palette below is used instead.

        Status/navigation bar contrast (light vs. dark icons) is handled
        automatically at runtime by EdgeToEdge.enable() in MainActivity, so it is
        intentionally not hardcoded here.
    -->
    <style name="Base.AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme" parent="Base.AppTheme" />
</resources>
